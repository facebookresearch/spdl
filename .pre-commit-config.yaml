default_language_version:
  node: 16.14.2

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.0.1
    hooks:
      - id: check-toml
      - id: check-yaml
      - id: end-of-file-fixer
        exclude_types:
          - "svg"

  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v21.1.2
    hooks:
      - id: clang-format
        exclude: docs

  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version. Pinned to the ruff bundled in fbsource's ruff-api==0.2.1
    # (tools/lint/pyfmt/reqs/requirements-fmt.txt) so OSS pre-commit and
    # internal `arc lint` agree. Verify with:
    #   python -c "import ruff_api; print(ruff_api.ruff_version)"
    rev: v0.14.3
    hooks:
      # Run the linter.
      - id: ruff
        files: "src/|tests/|examples/|.github/"
        exclude: "src/spdl/io/lib/.+.pyi"
        types_or: [ python, pyi ]
        args: [ "--fix" ]
      # Run the formatter.
      - id: ruff-format
        files: "src/|tests/|examples/|.github/"
        exclude: "src/spdl/io/lib/.+.pyi"
        types_or: [ python, pyi ]

  # This check is set to never fail, but will print all current mypy failures. See:
  # https://stackoverflow.com/a/59745587
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.16.1
    hooks:
      - id: mypy
        files: "src/|tests/|examples/"
        verbose: true
        entry: bash -c
        args:
          # Call mypy on all files that aren't marked "pyre-unsafe", excluding 'setup.py' files.
          - 'mypy $((grep -L "# pyre-unsafe" "$@" 2>/dev/null || printf "%s\n" "$@") | grep -v setup.py) || true'
          - --
        types_or: [python, pyi]
