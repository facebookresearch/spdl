[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "ninja",
    "cmake>=3.24",
]
build-backend = "setuptools.build_meta"

[tool.ruff]

extend-exclude = [
  "setup.py",
  "docs/source",
  "third_party",
  "setup.py",
]
